#include "lib_funcs.h"
/**
 * path_handler - handle path.
 * @command: string
 * Return: a strig.
 */
char *path_handler(char *command)
{
char *pth;
char *cpth;
int length;
char *tok;
pth = _getenv("PATH");
cpth = _strdup(s);
length = _strlen(command);
tok = strtok(cpth, ":");
free(cp);
}